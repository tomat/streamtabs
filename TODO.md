# TODO

- [x] Deselect the selected row when it is clicked again (keyboard `d` should also deselect).
- [x] Investigate screenshot bug where the `(paused)` label appears clipped on the right.
- [x] Investigate screenshot/log rendering issue where some log lines are cut off in captured images.
